{
    "question_id": "QT48DD6D13",
    "title": "Merge Two Sorted Lists",
    "difficulty": "EASY",
    "statement": "You are given the heads of two sorted linked lists `list1` and `list2`. Merge the two lists into a single sorted linked list and return its head. The new list should be made by splicing together the nodes of the first two lists.\n\n**Input**\n- The input format is not required for this problem; you will implement the function directly.\n\n**Output**\n- Return the head of the merged linked list.\n\n**Constraints**\n- The number of nodes in each list is in the range [0, 50].\n- -100 ≤ Node.val ≤ 100\n- Both `list1` and `list2` are sorted in non‑decreasing order.\n\n**Examples**\n*Example 1*\nInput: list1 = [1,2,4], list2 = [1,3,4]\nOutput: [1,1,2,3,4,4]\n\n*Example 2*\nInput: list1 = [], list2 = []\nOutput: []\n\n*Example 3*\nInput: list1 = [], list2 = [0]\nOutput: [0]\n\n**Function Signature**\n```python\n# Definition for singly‑linked list.\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef mergeTwoLists(list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:\n    # return the head of the merged list\n```\n**Explanation of Expected Solution**\nIterate through both lists with two pointers, repeatedly attaching the smaller node to the result list. When one list is exhausted, attach the remainder of the other list.\n\n**Correctness Proof Sketch**\nAt each step the smallest remaining node among the two lists is chosen, preserving order. Because the input lists are individually sorted, the produced list is sorted. All nodes are used exactly once.\n\n**Complexity Analysis**\nTime: O(n + m) where n and m are the lengths of the two lists.\nSpace: O(1) extra (in‑place).\n\n**Reference Implementation (Python 3)**\n```python\ndef mergeTwoLists(list1, list2):\n    dummy = ListNode()\n    tail = dummy\n    while list1 and list2:\n        if list1.val < list2.val:\n            tail.next = list1\n            list1 = list1.next\n        else:\n            tail.next = list2\n            list2 = list2.next\n        tail = tail.next\n    tail.next = list1 if list1 else list2\n    return dummy.next\n```\n",
    "summary": "Merge two sorted singly‑linked lists into one sorted list.",
    "constraints": [
        "0 ≤ length of each list ≤ 50",
        "-100 ≤ Node.val ≤ 100"
    ],
    "examples": [
        {
            "input": "list1 = [1,2,4], list2 = [1,3,4]",
            "output": "[1,1,2,3,4,4]",
            "explanation": ""
        },
        {
            "input": "list1 = [], list2 = []",
            "output": "[]",
            "explanation": ""
        },
        {
            "input": "list1 = [], list2 = [0]",
            "output": "[0]",
            "explanation": ""
        }
    ],
    "testcases": [],
    "topics": [
        "Linked List",
        "Two Pointers"
    ],
    "io_template": [
        "Function receives two linked‑list heads",
        "Return the head of the merged list.",
        ""
    ],
    "time_limit": 1000
}